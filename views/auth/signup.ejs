<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <nav class="navigation" id="navbar">
        <div id="logo">
            <img src="/img/logo.png" alt="Amitex Food">
        </div>
        <ul id="navList">
            <li class="navListItem"><a href="/">Home</a></li>
            <li class="navListItem"><a href="/products">Products</a></li>
            <% if(!isAdmin&&isAuthenticated){ %> 
            <li class="navListItem"><a href="">Cart</a></li>
            <li class="navListItem"><a href="">Your Orders</a></li>
            <li class="navListItem"><a href="">Edit Profile</a></li>
            <% } %> 
            <% if(isAdmin&&isAuthenticated){ %> 
                <li class="navListItem"><a href="/admin-product-list">Admin Products</a></li>
                <li class="navListItem"><a href="/add-product">Add Products</a></li>
                <% } %> 
            

        </ul>
        <div class="authList">
            <ul id="authList">
                <% if(!isAuthenticated){ %> 
                <li class="authListItem"><a href="/login">Login</a></li>
                <li class="authListItem"><a href="/signup" class="active">Signup</a></li>
                <% } %> 
                <% if(isAuthenticated){ %> 
                <li class="authListItem"> <form action="/logout" method="post">
                    <button type="submit">logout</button>
                </form></li>
                <% } %> 
                <li class="authListItem"><a href="/feedback">Feedback</a></li>
                
            </ul>
        </div>
    </nav>
    
    <div class="authContainer">
        <form class="authForm" action="/signup" method="post">
            <% if(errorMessage){ %> 
            <h1 id="errorMessage"><%= errorMessage %> </h1>
            <% } %> 
            <h1 id="authHeading">
                Please enter your credentials
            </h1>
            <h3>Name</h3>
            <input type="text" name="name" id="name" placeholder="Name" value="<%= oldInput.name %> ">
            <h3>Email</h3>
            <input type="email" name="email" id="email" value="<%= oldInput.email %> ">
            <h3>Password</h3>
            <input type="password" name="password" id="password" value="<%= oldInput.password %>">
            <h3>Contact Number</h3>
            <input type="number" name="contactNumber" id="contactNumber" value=<%= oldInput.contactPassword %>>
            <h3>Address</h3>
            <input type="text" name="address" id="address" value="<%= oldInput.address %> ">
            <button id="authSubmit" type="submit">Sign Up</button>
    
        </form>
    </div>
</body>
</html>