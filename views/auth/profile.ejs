<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
    <nav class="navigation" id="navbar">
        <div id="logo">
            <img src="/img/logo.png" alt="Amitex Food">
        </div>
        <ul id="navList">
            <li class="navListItem"><a href="/">Home</a></li>
            <li class="navListItem"><a href="/products">Products</a></li>
            <% if(!isAdmin&&isAuthenticated){ %> 
            <li class="navListItem"><a href="/cart">Cart</a></li>
            <li class="navListItem"><a href="/orders">Your Orders</a></li>
            <li class="navListItem"><a href="/profile" class="active">Your Account</a></li>
            <% } %> 
            <% if(isAdmin&&isAuthenticated){ %> 
                <li class="navListItem"><a href="/admin-product-list">Admin Products</a></li>
                <li class="navListItem"><a href="/add-product">Add Products</a></li>
                <% } %> 
            

        </ul>
        <div class="authList">
            <ul id="authList">
                <% if(!isAuthenticated){ %> 
                <li class="authListItem"><a href="/login">Login</a></li>
                <li class="authListItem"><a href="/signup" class="active">Signup</a></li>
                <% } %> 
                <% if(isAuthenticated){ %> 
                <li class="authListItem"> <form action="/logout" method="post">
                    <button type="submit">Logout</button>
                </form></li>
                <% } %> 
                <li class="authListItem"><a href="/feedback">Feedback</a></li>
                
            </ul>
        </div>
    </nav>
    <div class="profileContainer">
        <div class="profileGenerator">
            <div id="profileName">
                <h3>Name : <%= user.name %> </h3>
            </div>
            <div id="profileEmail">
                <h3>Email : <%= user.email %> </h3>
            </div>
            <div id="profileContactNumber">
                <h3>Contact Number : <%= user.contactNumber %> </h3>
            </div>
            <div id="editProfileBtn">
                <form action="/edit-profile" method="get">
                    <button type="submit">Edit Profile</button>
                </form>
                <form action="/address" method="get">
                    <button type="submit">My Addresses</button>
                </form>
            </div>
        </div>
        
           
    </div>
</body>
</html>